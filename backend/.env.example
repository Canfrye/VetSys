# PostgreSQL bağlantı adresi (gerçek kimlik bilgilerini .env dosyanıza girin, .env asla commit edilmemelidir)
DATABASE_URL="postgresql://vetsys:vetsys@localhost:5432/vetsys?schema=public"

NODE_ENV=development
PORT=4000

JWT_SECRET="change-this-to-a-long-random-secret"
JWT_EXPIRES_IN="8h"

CORS_ORIGIN="http://localhost:5173"
